System.Text.Json 源生成器（`JsonSerializerContext` + `[JsonKnownNamingPolicy.CamelCase]`）用于高性能 JSON 序列化；CommunityToolkit.Mvvm 的 `ObservableObject`、`[ObservableProperty]`、`SetProperty` 实现属性通知；Avalonia `Dispatcher.UIThread` 提供 UI 线程调度。